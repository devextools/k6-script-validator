The `hooks` folder contains git hooks.

Folders `hooks/partials` and `hooks/shared` are generated by Gookme on initialization and can store reusable scripts.

See [Gookme documentation](https://lmaxence.github.io/gookme/features/) for more details. 